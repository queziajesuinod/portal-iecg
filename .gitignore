# Don't check auto-generated stuff into git
coverage
build
node_modules
stats.json
.env
Dockerfile
# Cruft
.DS_Store
npm-debug.log
.idea
dock.yaml
homologacao-153895-portaliecghomo.p12

# Audios uploadados (Whisper local). DEFAULT: cwd/uploads/audios/
# Em prod, configure AUDIO_STORAGE_PATH apontando pra fora do projeto.
uploads/
../dados.scheduler-state.json
